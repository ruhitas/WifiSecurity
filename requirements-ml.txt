# Machine-learning stack (Phase 6+). Installed separately (large downloads).
# NOTE: this machine has NO NVIDIA GPU, so CPU builds are used. GPU-accelerated
# training/inference (Phase 9/10) requires GPU hardware or a cloud node.
numpy>=1.26,<2.2
pandas>=2.2
scikit-learn>=1.5
mlflow==2.17.2
# CPU-only deep-learning builds:
torch --index-url https://download.pytorch.org/whl/cpu
tensorflow-cpu>=2.17
